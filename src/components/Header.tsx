export function Header() {
  return (
    <header className="mast">
      <div className="mast-brand">
        <span className="mark" aria-hidden="true" />
        <div>
          <p className="eyebrow">SMF Works · Human-AI lab</p>
          <h1>Redact Before Share</h1>
        </div>
      </div>
      <p className="lede">
        Paste a transcript. Scrub secrets in the browser. Export clean text plus
        a removal receipt — counts and placeholders, never the values.
      </p>
    </header>
  );
}
